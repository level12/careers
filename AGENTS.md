# Docs formatting

- Preserve GitHub admonition syntax in Markdown.
- Keep lines like `> [!NOTE]` on their own line and fix it content comes after
  them.
- Markdown files (`*.md`): run `dprint fmt` when finished with all edits.
